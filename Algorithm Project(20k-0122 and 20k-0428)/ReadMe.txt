This sorting visualizer shows 10 sorts
but when calling bucket sort we have to change the file in draw_bar where list is being generated.
change file to number_b.txt and change the next line to float.

